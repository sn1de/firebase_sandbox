== README

Simple firebase application. Handles a list of counters. Uses
child_changed and child_added events to update the list
of all counters. Counter increments are done using firebase
transactions.
